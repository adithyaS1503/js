const arrNumbers = [1, 2, 3, 4];
console.log(arrNumbers);

var otherWayArr = Array(2); // stupid way of creating arrays where 2 is the length of the array
console.log(otherWayArr);


const arrQ = [];

const chrData = [
    obj1 = {name: 'Something', age: 30},
    obj2 = {name: 'Someone', age: 20}
];

arrQ.push(chrData[0]);
console.log(arrQ);

console.log(typeof("Will it return object?",chrData))
